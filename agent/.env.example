# Telegram: @Quazar_Agent_Bot (bot_id=8984617018)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# SSH SOCKS-шлюз на этой машине (ssh -D 1080). Авто-определяется, если порт открыт.
TELEGRAM_PROXY=socks5h://127.0.0.1:1080

QUAZAR_MASTER_KEY=QUAZAR_MASTER_KEY_2026
QUAZAR_NODE_SECRET=QUAZAR_NODE_SECRET_2026

# Узлы для мониторинга: name=url через запятую
QUAZAR_WATCH_NODES=local=http://127.0.0.1:8080,node2=http://127.0.0.1:8081

# Алерты о новых блоках — только с этих узлов (node2 при sync дублирует local)
WATCHER_BLOCK_ALERT_NODES=local

WATCHER_INTERVAL_SECS=30
WATCHER_PENDING_ALERT_THRESHOLD=10
WATCHER_PENDING_STALL_SECS=120
